apiVersion: v2
name: demo-java
description: demo-java
type: application
version: 0.1.0
appVersion: "0.1.0"
# Consome o arquétipo central por versão (bump via Renovate).
dependencies:
  - name: backend
    version: "2.6.0"
    repository: oci://ghcr.io/tourinhom/charts
